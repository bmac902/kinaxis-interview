{
  "name": "kinaxis-finops",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,vite -c cyan,blue \"npm run dev --prefix server\" \"npm run dev --prefix dashboard\"",
    "install:all": "npm install --prefix server && npm install --prefix dashboard"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
